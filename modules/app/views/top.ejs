<html>
  <head>
    <title>Blog</title>
    <link rel='stylesheet' href='/styles/style.css' />
    <link rel='stylesheet' href='/bootstrap/css/bootstrap.min.css' />
  </head>
  <body>
    <div class="container wrapper">
      <div class="row">
        <div class="col-sm-12">
          <nav class="navbar navbar-default navbar-fixed-top">
            <div class="container">
              <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                  <span class="sr-only">Toggle navigation</span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="/">Erishev Artem</a>
              </div>
              <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                  <% if (user) { %>
                    <p class="navbar-text navbar-right">Signed in as <a href="#" class="navbar-link"><%=user.username%></a></p>
                    <li><a href="" onclick="$('<form method=POST action=/logout>').submit();return false">Sign out</a></form></li>
                  <% } else { %>
                    <button type="button" class="btn btn-default navbar-btn btn-registration">Registration</button>
                    <button type="button" class="btn btn-default navbar-btn btn-login">Sign in</button>
                  <% } %>
                </ul>
              </div>
            </div>
          </nav>
